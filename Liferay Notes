-)what is Liferay?
liferay is a java based open source corporate portal developed by liferay incorporation starting from the version 7.x it is 
called as liferay DXP .There is two type's of version first is community version and second is DXP version.

-)what is themeDisplay?
ThemeDisplay is a helper class that provide all the information about the current portal context 
including logged in user,site,page,company and theme details.

site(Group)
Page(Layout)
portalURL-Domain of the Portal.
themeDisplay.isSignedIn= return false if user is not logged in 
                         return true if user id logged in 
-)Portlet Reference 
Portlet Reference means reuse existing Portlet instead of creating a new one 

-)PropsUtil
PropsUtil is a utility class in liferay used to read propertie's from the liferay's confrigstion's files like 
portal.properties, system.properties and custom .properties files.
